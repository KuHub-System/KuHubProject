package KuHub.modules.gestion_soporte.entity;

/** Tipo de equipo desde el que el usuario reporta. Coincide con el enum nativo tipo_equipo_soporte. */
public enum TipoEquipoSoporte {
    NOTEBOOK,
    DESKTOP,
    TABLET,
    OTRO
}
